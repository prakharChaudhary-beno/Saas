// Sirf yeh 2 lines change karni hain:
// javascript// auth.controller.js mein googleCallback

// // ❌ Abhi — JSON return kar rahe hain (testing ke liye)
// return res.status(200).json({
//   success: true,
//   message: "New user — complete registration",
//   isNewUser: true,
//   tempToken
// });

// // ✅ Jab frontend ready ho — redirect karo
// return res.redirect(
//   `${process.env.FRONTEND_URL}/onboarding?token=${tempToken}`
// );
// javascript// Existing user ke liye bhi

// // ❌ Abhi — JSON return
// return res.status(200).json({ success: true, ... });

// // ✅ Jab frontend ready ho — redirect
// return res.redirect(
//   `${process.env.FRONTEND_URL}/dashboard?token=${token}`
// );

// .env update karo:
// bash# Abhi
// FRONTEND_URL=http://localhost:3001  # ya jo bhi frontend port ho

// # Production mein
// FRONTEND_URL=https://app.yourdomain.com

// Bas itna — baaki sab backend same rahega. 🎯


employeeCredentials
        "credentials": {
            "email": "alt.d9-43nfhrv@yopmail.com",
            "password": "6bswq2ze"
        }